
# =========================================
# SynOS v2.0 Git Ignore Configuration
# Updated: October 22, 2025 - Post AI Subsystem Reorganization
# Optimized for: src/ai/ structure, Rust (992 files), Python (8344 files)
# =========================================

# =========================================
# BUILD OUTPUTS
# =========================================
build/
dist/
out/
target/
*.iso
*.img
*.squashfs

# Additional build artifacts
*.vdi
*.vmdk
*.qcow2
*.bin
*.elf
*.o
*.ko
*.so
*.a
*.deb
Cargo.lock.orig

# Build caches and bootstrap directories
linux-distribution/SynOS-Linux-Builder/cache/
linux-distribution/SynOS-Linux-Builder/*/chroot/
*.pwd.lock

# Workspace directories (can be multi-GB)
build/workspace-*/
scripts/build/*/chroot/

# Large archives and ISO content
Final_SynOS*/
squashfs_extracted/
*_extracted/
initrd.img*
vmlinuz*

# =========================================
# LOGS
# =========================================
logs/
*.log

# =========================================
# TEMPORARY FILES
# =========================================
tmp/
temp/
*.tmp
*.bak
*.backup
*~
*.swp
*.swo

# =========================================
# SECURITY: SECRETS & CREDENTIALS (CRITICAL)
# =========================================
*.env
.env
.env.*
!.env.example
!.env.template
!*.env.example
*secret*
*credential*
*.pem
*.key
!example.key
!template.key
!public.key
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
*.ppk
*.p12
*.pfx
*.keystore
*.jks

# =========================================
# SECURITY: SENSITIVE DOCUMENTATION (Git-Crypt Protected)
# =========================================
# NOTE: These directories use git-crypt for encryption
# Files are encrypted in Git but readable when unlocked with GPG key
# Uncomment below ONLY for public repository (to completely exclude)

# For PRIVATE repository (current): Use git-crypt encryption (files tracked but encrypted)
# docs/wiki/internal/       # Uncomment to completely exclude from Git
# docs/wiki/restricted/     # Uncomment to completely exclude from Git

# For PUBLIC repository: Uncomment above to prevent any commits

# Security documentation (allow README, exclude secrets)
docs/wiki/security/*
!docs/wiki/security/README.md
!docs/wiki/SECURITY.md

# Backup files (encrypted backups of sensitive docs)
*wiki-backup-*.tar.gz.gpg
*wiki-internal-*.tar.gz.gpg
*wiki-restricted-*.tar.gz.gpg
synos-backups/

# GPG key exports (never commit private keys!)
*-private-key.asc
*-secret-key.asc
*.gpg-key

# =========================================
# DATABASES
# =========================================
*.db
*.sqlite
*.sqlite3
*.db-journal

# =========================================
# IDE & EDITORS
# =========================================
# VS Code
.vscode/
.vscode/settings.json
.vscode/launch.json
.vscode/tasks.json
.vscode/.ropeproject/
.vscode/extensions.json
.vscode/*.backup*
.vscode/*codespace-backup*
.vscode-test/

# JetBrains IDEs
.idea/
*.iml
*.ipr
*.iws

# Other editors
*.sublime-project
*.sublime-workspace
.vs/

# =========================================
# PYTHON (8344 files: AI daemons, tools, scripts)
# =========================================
__pycache__/
*.pyc
*.pyo
*.pyd
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
ENV/
.env/
*.egg
*.egg-info/
dist/
*.whl
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.dmypy.json
dmypy.json

# AI daemon specific
src/ai/daemons/**/__pycache__/
src/ai/daemons/**/*.pyc

# Python virtual environments (project-specific)
scripts/.venv/
development/.venv/
tools/.venv/
**/.venv/

# =========================================
# NODE.JS / JAVASCRIPT
# =========================================
node_modules/
.npm/
.pnpm-store/
.cache/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# =========================================
# RUST (992 files: kernel, AI engine, AI runtime, core services)
# =========================================
Cargo.lock
**/*.rs.bk
*.pdb
# Rust target directories (massive - can be GB)
**/target/
# Cargo build artifacts
.cargo/
!.cargo/config.toml
!.cargo/audit.toml

# Nested target directories (use workspace target instead)
src/kernel/target/
src/userspace/*/target/

# Fuzz artifacts
fuzz/corpus/
fuzz/artifacts/

# Large audit reports
workspace-audit-report.json
*-audit-report.json

# =========================================
# DOCUMENTATION BUILDS
# =========================================
docs/_build/
site/
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# =========================================
# CONTAINER & ORCHESTRATION
# =========================================
docker-compose.override.yml
.dockerignore.local
*.tar
*.tar.gz
*.tgz

# =========================================
# OS FILES
# =========================================
.DS_Store
Thumbs.db
Desktop.ini
$RECYCLE.BIN/
*.lnk
.fuse_hidden*
.directory
.Trash-*

# =========================================
# AI MODEL FILES & DATA (TensorFlow Lite, ONNX, PyTorch)
# =========================================
# Trained model files (use Git LFS if needed)
*.onnx
*.tflite
*.pb
*.pth
*.h5
*.keras
*.model
*.weights
*.ckpt

# Training data and datasets
*.pkl
*.pickle
*.npy
*.npz
*.hdf5
*.tfrecord

# Model checkpoints and training artifacts
checkpoints/
model_outputs/
training_logs/
tensorboard_logs/
mlruns/

# =========================================
# AI RUNTIME LIBRARIES (Installed System-Wide)
# =========================================
# TensorFlow Lite library (4.4MB installed at /usr/local/lib/)
libtensorflowlite*
# ONNX Runtime library (to be installed)
libonnxruntime*
# PyTorch LibTorch library (to be installed)
libtorch*
# AI accelerator drivers
*.so.ai
*.so.npu

# =========================================
# SYSTEMD & LINUX DISTRIBUTION (587 service files)
# =========================================
# Systemd journal files
*.journal
systemd-private-*
/var/log/journal/

# Linux distribution build artifacts
linux-distribution/SynOS-Linux-Builder/packages/*.deb
linux-distribution/SynOS-Linux-Builder/custom-repo/*.deb
!linux-distribution/SynOS-Linux-Builder/custom-repo/synos-*.deb

# Chroot environments (can be multi-GB)
*/chroot/
airootfs/work/

# =========================================
# CERTIFICATES & SSH (IMPORTANT)
# =========================================
*.crt
*.csr
!example.crt
!test.crt
authorized_keys
known_hosts

# =========================================
# AUDIO FILES (ALFRED Voice Assistant)
# =========================================
# Recorded audio samples (use Git LFS if needed)
*.wav
*.mp3
*.ogg
*.flac
# Audio cache
.audio_cache/
audio_samples/temp/

# =========================================
# NATS MESSAGE BUS & SERVICES
# =========================================
# NATS data directory
nats-data/
*.dat
*.blk
# Service runtime data
services/runtime/
*.pid
