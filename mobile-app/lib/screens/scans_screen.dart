import 'package:flutter/material.dart';

class ScansScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Active Scans')),
      body: Center(child: Text('Scans Screen - TODO')),
    );
  }
}
