#!/bin/bash
set -e

echo "Setting up AI engine..."

# Install Python AI dependencies (will be installed in virtual environment on first boot)
# These are just placeholders - actual installation happens via venv
echo "AI dependencies will be installed on first boot via virtual environment"

# Create AI model directory
mkdir -p /opt/synos/models
chown -R synos:synos /opt/synos/models 2>/dev/null || true

echo "✓ AI engine configured"

exit 0
