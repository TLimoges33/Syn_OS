# GitHub Personal Access Token Configuration
# Copy this to .env.tokens and add your actual token
# NEVER commit .env.tokens to version control

# GitHub Personal Access Token
GITHUB_TOKEN=ghp_your_token_here

# GitHub CLI Authentication
GH_TOKEN=$GITHUB_TOKEN

# For GitHub Actions and CI/CD
GITHUB_API_TOKEN=$GITHUB_TOKEN

# Repository access
GITHUB_REPO_TOKEN=$GITHUB_TOKEN

# Usage examples:
# git clone https://$GITHUB_TOKEN@github.com/username/repo.git
# gh auth login --with-token <<< "$GITHUB_TOKEN"
# curl -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/user
