[package]
name = "syn-consciousness"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
# Core no-std dependencies
spin = { workspace = true }

# Error handling - no-std compatible
anyhow = { workspace = true }

# Serialization - no-std compatible
serde = { workspace = true }

[features]
default = ["local-inference", "decision-engine", "pattern-recognition"]
local-inference = []
decision-engine = []
pattern-recognition = []
