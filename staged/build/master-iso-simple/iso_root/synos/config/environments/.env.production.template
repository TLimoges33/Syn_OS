# Syn_OS Production Environment Configuration
# CRITICAL: Replace all default values with production secrets

# =============================================================================
# CORE SYSTEM CONFIGURATION
# =============================================================================

# Environment
ENV=production
LOG_LEVEL=WARN
DEBUG=false

# =============================================================================
# DATABASE CONFIGURATION - PRODUCTION SECRETS REQUIRED
# =============================================================================

# PostgreSQL - REPLACE WITH PRODUCTION SECRETS
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=syn_os_prod
POSTGRES_USER=syn_os_prod_user
POSTGRES_PASSWORD=REPLACE_WITH_STRONG_PRODUCTION_PASSWORD
POSTGRES_SSL_MODE=require

# =============================================================================
# CACHE CONFIGURATION - PRODUCTION SECRETS REQUIRED
# =============================================================================

# Redis - REPLACE WITH PRODUCTION SECRETS
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=REPLACE_WITH_STRONG_REDIS_PASSWORD
REDIS_DB=0

# =============================================================================
# MESSAGE BUS CONFIGURATION - PRODUCTION SECRETS REQUIRED
# =============================================================================

# NATS Configuration - REPLACE WITH PRODUCTION SECRETS
NATS_URL=nats://nats:4222
NATS_CLUSTER_ID=syn-os-prod-cluster
NATS_CLIENT_ID=syn-os-prod-client
NATS_USERNAME=orchestrator_prod
NATS_PASSWORD=REPLACE_WITH_STRONG_NATS_PASSWORD
NATS_MAX_RECONNECT=10

# NATS JetStream Configuration
NATS_JETSTREAM_ENABLED=true
NATS_STREAM_REPLICAS=3
NATS_MAX_MEMORY_STORE=4GB
NATS_MAX_FILE_STORE=50GB

# NATS Subject Configuration
NATS_CONSCIOUSNESS_SUBJECT=consciousness.>
NATS_ORCHESTRATOR_SUBJECT=orchestrator.>
NATS_SECURITY_SUBJECT=security.>

# =============================================================================
# SERVICE CONFIGURATION
# =============================================================================

# Service Orchestrator
HTTP_PORT=8080
ORCHESTRATOR_URL=http://orchestrator:8080

# Consciousness Service
CONSCIOUSNESS_URL=http://consciousness:8081
CONSCIOUSNESS_MODE=production
CONSCIOUSNESS_ENCRYPTION_KEY=REPLACE_WITH_PRODUCTION_AES256_KEY

# Security Dashboard
SECURITY_DASHBOARD_PORT=8083

# Learning Hub
LEARNING_HUB_PORT=8084

# Security Tutor
SECURITY_TUTOR_PORT=8085

# Web Dashboard
WEB_DASHBOARD_PORT=8086

# Threat Intelligence Dashboard
THREAT_INTEL_PORT=8087

# =============================================================================
# SECURITY CONFIGURATION - CRITICAL PRODUCTION SECRETS
# =============================================================================

# JWT Configuration - REPLACE WITH PRODUCTION SECRETS
JWT_SECRET_KEY=REPLACE_WITH_PRODUCTION_JWT_SECRET_MINIMUM_64_CHARACTERS
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRE_DAYS=1

# Encryption - REPLACE WITH PRODUCTION SECRETS
ENCRYPTION_KEY=REPLACE_WITH_PRODUCTION_AES256_KEY
SIGNING_KEY=REPLACE_WITH_PRODUCTION_HMAC_KEY

# Internal API Security - REPLACE WITH PRODUCTION SECRETS
INTERNAL_API_KEY=REPLACE_WITH_PRODUCTION_INTERNAL_API_KEY

# =============================================================================
# AI/ML CONFIGURATION - REPLACE WITH PRODUCTION KEYS
# =============================================================================

# LM Studio Integration
LM_STUDIO_API_KEY=REPLACE_WITH_PRODUCTION_LM_STUDIO_KEY
LM_STUDIO_URL=http://localhost:1234

# OpenAI Integration (optional)
OPENAI_API_KEY=REPLACE_WITH_PRODUCTION_OPENAI_KEY

# Anthropic Integration (optional)
ANTHROPIC_API_KEY=REPLACE_WITH_PRODUCTION_ANTHROPIC_KEY

# Google AI Integration (optional)
GOOGLE_AI_API_KEY=REPLACE_WITH_PRODUCTION_GOOGLE_AI_KEY

# =============================================================================
# MONITORING AND OBSERVABILITY
# =============================================================================

# Prometheus
PROMETHEUS_PORT=9090

# Grafana - REPLACE WITH PRODUCTION CREDENTIALS
GRAFANA_PORT=3001
GRAFANA_ADMIN_PASSWORD=REPLACE_WITH_STRONG_GRAFANA_PASSWORD

# Health Check Configuration - Production Optimized
HEALTH_CHECK_INTERVAL=15
HEALTH_CHECK_TIMEOUT=5
HEALTH_CHECK_RETRIES=3

# =============================================================================
# PRODUCTION CONFIGURATION
# =============================================================================

# Production flags
DEVELOPMENT_MODE=false
SYN_OS_DEV_ENV=false
SECURITY_ENABLED=true

# Production URLs (if using external load balancer)
REACT_APP_ORCHESTRATOR_URL=https://your-domain.com/api
REACT_APP_CONSCIOUSNESS_URL=https://your-domain.com/consciousness
REACT_APP_API_KEY=${INTERNAL_API_KEY}

# =============================================================================
# NATS PRODUCTION CONFIGURATION
# =============================================================================

# NATS Connection Pool - Production Optimized
NATS_CONNECTION_POOL_SIZE=20
NATS_CONNECTION_TIMEOUT=10s
NATS_RECONNECT_WAIT=2s

# NATS Message Configuration - Production Optimized
NATS_MESSAGE_MAX_SIZE=10MB
NATS_MESSAGE_TTL=1h
NATS_ACK_WAIT=10s

# NATS Monitoring
NATS_MONITORING_PORT=8222
NATS_SURVEYOR_PORT=7777

# =============================================================================
# CONSCIOUSNESS SYSTEM PRODUCTION CONFIGURATION
# =============================================================================

# Neural Darwinism Settings - Production Optimized
NEURAL_POPULATION_SIZE=500
NEURAL_MUTATION_RATE=0.05
NEURAL_SELECTION_PRESSURE=0.9

# Consciousness Processing - Production Optimized
CONSCIOUSNESS_PROCESSING_THREADS=8
CONSCIOUSNESS_MEMORY_LIMIT=4GB
CONSCIOUSNESS_CACHE_SIZE=10000

# Learning and Adaptation - Production Optimized
LEARNING_RATE=0.001
ADAPTATION_THRESHOLD=0.85
CONTEXT_WINDOW_SIZE=5000

# =============================================================================
# SSL/TLS CONFIGURATION
# =============================================================================

# SSL Certificates
SSL_CERT_PATH=/etc/nginx/certs/syn_os.crt
SSL_KEY_PATH=/etc/nginx/certs/syn_os.key
SSL_CA_PATH=/etc/nginx/certs/ca.crt

# SSL Configuration
SSL_PROTOCOLS=TLSv1.2 TLSv1.3
SSL_CIPHERS=ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384

# =============================================================================
# BACKUP AND DISASTER RECOVERY
# =============================================================================

# Backup Configuration
BACKUP_ENABLED=true
BACKUP_SCHEDULE="0 2 * * *"
BACKUP_RETENTION_DAYS=30
BACKUP_STORAGE_PATH=/backups

# =============================================================================
# SECURITY HEADERS AND POLICIES
# =============================================================================

# Content Security Policy
CSP_ENABLED=true
HSTS_ENABLED=true
FRAME_OPTIONS=DENY
CONTENT_TYPE_OPTIONS=nosniff
